const myWeb = new Vue({
  el: '#root',
  data: {
    mainMenu: [...mainMenu],
    socialIcons: [...socialIcons],
    sectionNumbers: [...sectionNumbers],
    onlineCourses: [...onlineCourses],
  },
});
